apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
commonLabels:
  app: dex
  component: dex

resources:
- deployment.yaml
- route.yaml
- sa.yaml
- service.yaml
- pvc.yaml
- imagestream.yaml

configMapGenerator:
- name: dex-config
  files:
  - config.yaml=files/dex-config.yaml
